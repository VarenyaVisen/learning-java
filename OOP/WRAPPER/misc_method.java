package WRAPPER;

public class misc_method {
    public static void main(String[] args){

        // This wapper method is used to check wether the character is a letter or not
        char letter = 'b';
        System.out.println(Character.isLetter(letter));
        System.out.println(Character.isUpperCase(letter));

        // Array List only work with objects so we need wrapper classes
        // It provides useful static utilities we can access

    }
}
